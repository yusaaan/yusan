RewriteEngine On

# Redirect the base URL to index.html (for homepage)
RewriteRule ^$ /index.html [L]

# Remove .html extension from the about page
RewriteRule ^about$ /about.html [L]

# Remove .html extension from the contact page
RewriteRule ^contact$ /contact.html [L]
